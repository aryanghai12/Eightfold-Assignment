"""Multi-Source Candidate Data Transformer.

Turns messy candidate inputs (CSV, ATS JSON, recruiter notes, resumes, GitHub) into one
clean, canonical, deduplicated profile per candidate — with provenance and confidence —
and projects that canonical record into any caller-requested output shape.
"""

__version__ = "1.0.0"
